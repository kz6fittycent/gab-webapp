name: gab
version: '1.0'
summary: A webapp for Gab.com
description: |
  A webapp for Gab.com
  
license: MIT

assumes:
  - command-chain
  
base: core18
grade: stable
confinement: strict

parts:
  gab:
    plugin: nil
    
  homeishome-launch:
    plugin: nil
    stage-snaps:
      - homeishome-launch
